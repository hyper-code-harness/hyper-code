/** Stages supported HealthKit sample batches through the private applehealth ingestion plugin. */
export default async function(ctx:Context,_session:Session|null,opts:{req:Request;params:Record<string,string>}){let body:any;try{body=await opts.req.json()}catch{return Response.json({error:"invalid_json",message:"Expected JSON"},{status:400})}const kinds=["sleep","heart_rate","blood_pressure","steps","active_energy","exercise_minutes","stand_hours","workout","resting_heart_rate","hrv","respiratory_rate","oxygen_saturation","body_mass","body_fat","distance"];if(!kinds.includes(body?.kind))return Response.json({error:"unsupported_kind",message:"Unsupported HealthKit sample kind"},{status:400});try{const result=await ctx.fns.applehealth.ingest({kind:body.kind,samples:body.samples});if(body.kind==="sleep")await ctx.fns.applehealth.promoteSleep({since:new Date(Date.now()-7*86400000).toISOString()});return Response.json({version:1,ok:true,...result})}catch(error:any){return Response.json({error:"invalid_samples",message:String(error?.message??error)},{status:400})}}
